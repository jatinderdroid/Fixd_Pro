<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.fixtconsumer.fragments.HomeFragment">

    <!-- TODO: Update blank fragment layout -->
    <ListView
        android:visibility="gone"
        android:id="@+id/lstTypePlan"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"></ListView>

    <LinearLayout
        android:paddingBottom="12dp"
        android:background="@color/theme_orange"
        android:layout_alignParentTop="true"
        android:orientation="vertical"
        android:gravity="center"
        android:id="@+id/service_location"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <TextView
            android:id="@+id/txtName"
            android:textSize="20sp"
            android:text="Hello GSh!"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/txtServiceFor"
            android:gravity="center_vertical"
            android:drawableRight="@drawable/home_location"

            android:textSize="18sp"
            android:text="Loading..."
            android:textColor="@android:color/white"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

    </LinearLayout>
    <View
        android:id="@+id/dividerView"
        android:layout_below="@+id/service_location"
        android:background="@android:color/white"
        android:layout_width="match_parent"
        android:layout_height="1.5dp"></View>
    <LinearLayout
        android:layout_below="@+id/dividerView"
        android:orientation="vertical"
        android:id="@+id/home_plans"
        android:layout_width="match_parent"
        android:layout_height="match_parent"></LinearLayout>
</RelativeLayout>
